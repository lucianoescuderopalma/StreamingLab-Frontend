# ── App ───────────────────────────────────────
spring.application.name=backend

# ── Servidor ──────────────────────────────────
server.port=8080

# ── PostgreSQL ────────────────────────────────
spring.datasource.url=jdbc:postgresql://localhost:5432/devroom_db
spring.datasource.username=postgres
spring.datasource.password=devroom123
spring.datasource.driver-class-name=org.postgresql.Driver

# ── JPA / Hibernate ───────────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ── Deshabilitar Redis, OAuth2 y Spring AI por ahora ─────
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration,\
  org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration,\
  org.springframework.ai.vectorstore.redis.autoconfigure.RedisVectorStoreAutoConfiguration./mvnw spring-boot:run

  # GitHub OAuth
github.client-id=Ov23liOZ8Db9SFAgin3l
github.client-secret=f73be3bf288fa991da00d766df6fc4386aef7cea
github.redirect-uri=http://localhost:5173/auth/callback

# JWT
jwt.secret=devroom-super-secret-key-2024-cambiar-en-produccion
jwt.expiration=86400000

# OpenAI (lo agregaremos después)
openai.api-key=TU_OPENAI_KEY_AQUI
openai.model=gpt-4o-mini

# Deshabilitar warning
spring.jpa.open-in-view=false